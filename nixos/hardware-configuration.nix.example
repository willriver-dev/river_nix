# Hardware Configuration - GENERATE THIS ON TARGET MACHINE
#
# This file is generated by nixos-generate-config and is specific to your hardware.
# 
# To generate this file on a NixOS machine:
#   sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix
#
# Or during installation from ISO:
#   nixos-generate-config --root /mnt
#
# This will create the actual hardware-configuration.nix with:
# - Boot loader settings
# - Filesystem mounts
# - Kernel modules
# - Hardware-specific options
#
# DO NOT COMMIT this file if it contains sensitive information!

{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [ ];

  # PLACEHOLDER - Replace with actual hardware config
  # Example structure (yours will be different):
  
  # boot.initrd.availableKernelModules = [ "xhci_pci" "ahci" "nvme" "usb_storage" "sd_mod" ];
  # boot.kernelModules = [ "kvm-intel" ];
  
  # fileSystems."/" = {
  #   device = "/dev/disk/by-uuid/YOUR-UUID-HERE";
  #   fsType = "ext4";
  # };
  
  # fileSystems."/boot" = {
  #   device = "/dev/disk/by-uuid/YOUR-BOOT-UUID";
  #   fsType = "vfat";
  # };
  
  # swapDevices = [ ];
  
  # networking.useDHCP = lib.mkDefault true;
  # nixpkgs.hostPlatform = lib.mkDefault "x86_64-linux";
  # hardware.cpu.intel.updateMicrocode = lib.mkDefault config.hardware.enableRedistributableFirmware;
}
